# Development environment file
# Copy this to .env and adjust values as needed

# Build target (development or production)
BUILD_TARGET=development

# Frontend API URL
VITE_API_URL=http://localhost:8000/api

# Backend configuration
DEBUG=True
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gtowizard
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://213.199.50.129:8089

# Django secret key (generate a new one for production)
SECRET_KEY=django-insecure-2o$=y)k^jj+n6pn=6metswzingj3pf5$^rq(9=tx0)+)_36d-z

# Optional: Docker registry configuration
# DOCKER_REGISTRY=your-registry.com
# DOCKER_USERNAME=your-username
# DOCKER_PASSWORD=your-password
